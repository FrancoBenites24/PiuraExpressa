package com.piuraexpressa.repositorio.dominio;

import com.piuraexpressa.model.dominio.TipoReaccion;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TipoReaccionRepositorio extends JpaRepository<TipoReaccion, Integer> {
}
